version: '3.8'
services:
  jupyter:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - '8200:8888'
    volumes:
      - .:/home/jovyan
    environment:
      - JUPYTER_ENABLE_LAB=True
